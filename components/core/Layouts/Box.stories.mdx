import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import Box from "./Box";

<Meta title="Core/Layouts/Box" component={Box}/>

# Box
Box is a general wrapper component for adding layouts

## Components
`<Box/>` Main component to act as the wrapper

## Props
`direction` props is omitted for all directional Components

<ArgsTable of={Box}/>

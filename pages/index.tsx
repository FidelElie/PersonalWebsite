import { type NextPage } from "next";

import { Page, Navbar } from "@/components/core";

const HomePage: NextPage = () => {
	return (
		<Page>
			<Navbar />

		</Page>
	)
}

export default HomePage;
